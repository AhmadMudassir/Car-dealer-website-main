import React from "react";
import ReactDOM from "react-dom";
// import './index.css'; // Ensure this CSS file exists or change the path if necessary
import App from "./App"; // Ensure this component exists or create it

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
